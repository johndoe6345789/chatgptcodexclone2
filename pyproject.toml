[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "codex-portable-desktop"
version = "0.9.0"
description = "Local ChatGPT/Codex-style coding assistant with a helper-process Tkinter desktop UI"
authors = [{ name = "Local User" }]
requires-python = ">=3.10"
dependencies = []

[tool.setuptools.packages.find]
where = ["codex_clone"]
